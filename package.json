{
  "name": "temacker-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-temacker \"jest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "jest": "^29.7.0"
  },
  "jest": {
    "testMatch": [
      "<rootDir>/firestore-tests/**/*.test.js"
    ]
  }
}
